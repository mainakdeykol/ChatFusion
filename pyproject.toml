[project]
name = "gradio-chatbot-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "gradio>=5.0.0",
    "openai>=1.0.0",
    "pydantic>=2.0.0",
]
